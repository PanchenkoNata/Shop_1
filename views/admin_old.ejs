<!DOCTYPE html>
<html>
  <head>
    <title><%= title %></title>
  </head>
  <body>
		<h2 class=""><%= title %></h2>
		    
    <form action="" method="POST" class="form static">
        <div class="form-group">
            <label for="category">Category</label>
            <input class="" type="text" name="category" placeholder="add a new category" minlength="2" maxlength="20" value="<%= data.category %>">
        </div>
        <div class="form-group">
            <label for="subcategory">Subcategory</label>
            <input class="" type="text" name="subcategory" placeholder="add a new subcategory" minlength="2" maxlength="20" value="<%= data.subcategory %>">
        </div>
        <div class="form-group">
            <label for="brand">Brand</label>
            <input class="" type="text" name="brand" placeholder="add a new brand" minlength="2" maxlength="20" value="<%= data.brand %>">
        </div>
        <div class="form-group">
            <label for="country">Country</label>
            <input class="" type="text" name="country" placeholder="add a new country" minlength="2" maxlength="20" value="<%= data.country %>">
        </div>
                
        <button class="btn" type="submit">submit</button>
    </form>

    <script src="/javascripts/signup.js"></script>
  </body>
</html>
